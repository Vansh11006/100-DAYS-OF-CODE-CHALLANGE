Q57: Find the sum of array elements.

/*
Sample Test Cases:
Input 1:
4
2 4 6 8
Output 1:
20

Input 2:
3
1 1 1
Output 2:
3

*/

ANS-
#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);   // read size of array

    int arr[n];        // declare array
    int sum = 0;       // variable to store sum

    // reading array elements
    for(int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
        sum += arr[i];   // add every element to sum
    }

    printf("%d", sum);   // print sum
    return 0;
}

 Input:

4
2 4 6 8
 Output:

20


Q58: Find the maximum and minimum element in an array.

/*
Sample Test Cases:
Input 1:
5
2 9 1 4 7
Output 1:
Max=9, Min=1

Input 2:
3
10 10 10
Output 2:
Max=10, Min=10

*/

ANS-

#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);   // read size of array

    int arr[n];
    
    // read array elements
    for(int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    int max = arr[0];
    int min = arr[0];

    // find max and min
    for(int i = 1; i < n; i++) {
        if(arr[i] > max)
            max = arr[i];
        if(arr[i] < min)
            min = arr[i];
    }

    printf("Max=%d, Min=%d", max, min);

    return 0;
}


 Input 1

5
2 9 1 4 7
Output

Max=9, Min=1
